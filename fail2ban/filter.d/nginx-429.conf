# nginx-429.conf — Fail2ban filter for rate limit violations
# Matches Nginx access log lines with HTTP 429 status

[Definition]
failregex = ^<HOST> .* "(GET|POST|PUT|DELETE|HEAD|OPTIONS) .* HTTP/[12]\.[01]" 429
ignoreregex =
