# nginx-404.conf — Fail2ban filter for 404 flood detection
# Matches Nginx access log lines with HTTP 404 status

[Definition]
failregex = ^<HOST> .* "(GET|POST|PUT|DELETE|HEAD|OPTIONS) .* HTTP/[12]\.[01]" 404
ignoreregex =
